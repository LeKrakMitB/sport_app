generator client {
  provider = "prisma-client-js"
  output   = "./generated"
}

datasource db {
  provider = "postgresql"
  url      = env("PAIEMENT_DATABASE_URL")
  schema   = ""
}

// TODO: D̩finir les mod̬les pour paiement
