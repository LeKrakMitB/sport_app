generator client {
  provider = "prisma-client-js"
  output   = "./generated"
}

datasource db {
  provider = "postgresql"
  url      = env("SELECTION_BOOKING_DATABASE_URL")
  schema   = ""
}

// TODO: D̩finir les mod̬les pour selection-booking
